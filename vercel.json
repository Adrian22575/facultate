{
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "npm run vercel:preflight && npm run build",
  "crons": [
    {
      "path": "/api/cron/learning-jobs",
      "schedule": "* * * * *"
    }
  ]
}
