import { RouteLoadingState } from "@/components/route-loading-state";

export default function ProgressLoading() {
  return <RouteLoadingState title="Pregatim progresul." description="Actualizam rezultatele si reperele tale de invatare." />;
}
